extends layout

block content
    p #{item.name} - #{item.size} oz
    p #{item.description}
    
    

    h4 Units Available

    ul
        each item in items
            li 
                p Lot #{item.lot} - #{item.bestby_formatted}
                
