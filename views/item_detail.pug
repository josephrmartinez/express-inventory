extends layout

block content
    p #{item.name} - #{item.size} oz - $#{item.price}
    p #{item.description}
    


    h4 Units Available

    ul
        each item in items
            li 
                p Lot #{item.lot} - #{item.bestby_formatted}
                
